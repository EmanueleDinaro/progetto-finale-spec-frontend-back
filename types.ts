export type Beer = {
  title: string;
  category: string; // Stile (belga, stout, IPA, Lager, ecc.)
  image: string;
  abv: number; // Gradazione alcolica
  color: string; // Bionda, ambrata, rossa, nera
  price: number;
  brewery: string; // Nome birrificio
  country: string; // Provenienza
  description: string;
};
